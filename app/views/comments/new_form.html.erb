<div class="form__container">
<%= tag.h1('New Comment', class: 'form__title') %>
<%= form_with model: [post, Comment.new] do |f| %>
  <%= f.text_area :text, rows: 10, placeholder: 'Enter your comment' %>
  <%= f.submit 'Save' %>
<% end %>
<%= render 'layouts/alert', flash: flash %>
</div>